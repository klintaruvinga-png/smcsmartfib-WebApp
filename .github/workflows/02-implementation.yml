name: 02 - Artifact Validation (Manual Only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest

    steps:
      - name: Explain disabled state
        run: |
          echo "02 - Artifact Validation is intentionally manual-only."
          echo "The authoritative automation pipeline runs locally via scripts/pipeline-watcher.js."
          echo "reports/copilot-research.md, reports/codex-plan.md, and .smc-workflow-state.json are local runtime artifacts and are not guaranteed to exist in GitHub."
